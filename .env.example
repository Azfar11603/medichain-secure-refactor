# .env.example — commit THIS file, never the real .env
# Copy to .env locally and fill in a real 32-byte base64 key.
# This file is referenced by crypto_vault.php via getenv('APP_CRYPTO_KEY').

APP_CRYPTO_KEY=base64:REPLACE_WITH_32_RANDOM_BYTES_BASE64_ENCODED
